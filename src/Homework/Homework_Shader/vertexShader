#version 330 core
layout (location = 0) in vec3 aPos;
uniform vec3 offset;
out vec3 aColor;

void main(){
    gl_Position = vec4(aPos.x+offset.x,aPos.y,aPos.z,1.0);
    aColor.x = abs(aPos.x);
    aColor.y = abs(aPos.y);
    aColor.z = abs(aPos.z);
}